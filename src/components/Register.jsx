import { Link } from "react-router-dom"
export default function Register() {
    return <div>
        <h1>Register to my Chat</h1>
        Register
        <Link to="/login">I already have an account!</Link>
    </div>
}